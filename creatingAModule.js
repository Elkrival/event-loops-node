/* We use the exports object in our code to export our functions and our methods to be used in our other applications*/
// 
// exports.concatArray= function(str, array) {
//   return array.map((element)=>{
//     return `${str} ${element}`
//   })
}//This code concatenates a string into each element of an array
/* to verify if it worked we import our library to our other pieces of code */
